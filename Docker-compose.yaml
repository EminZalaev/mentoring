version: "3.7"
services:
  database:
    image: postgres:latest
    restart: always
    env_file:
      - .env
    ports:
      - "5432"
  server:
    build: .
    links:
      - database
    ports:
      - "8080:8080"